export const metadata = {
  title: "Navbar Component Demo | EVENT NOW",
  description: "Demo page for the NavbarRight component",
}

export default function DemoLayout({ children }) {
  return <div>{children}</div>
}
